import { WhiteForbiddenDirective } from './white-forbidden.directive';

describe('WhiteForbiddenDirective', () => {
  it('should create an instance', () => {
    const directive = new WhiteForbiddenDirective();
    expect(directive).toBeTruthy();
  });
});
